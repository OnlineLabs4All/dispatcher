{% extends 'adminBase.html.twig' %}

{% block mainPanel %}

    <div id="header" class="col-lg-12">
        <h3 id="header3" class="page-header">Register a new Experiment Engine</h3>
    </div>

        {{ form_start(form) }}
        {{ form_errors(form) }}
         {{ form(form) }}
        {{ form_end(form) }}

{% endblock %}