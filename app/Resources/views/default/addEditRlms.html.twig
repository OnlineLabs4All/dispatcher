{% extends 'adminBase.html.twig' %}

{% block mainPanel %}

    {{ form_start(form) }}
    {{ form_row(form._token) }}

    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    {{ viewName }}
                </div>
                <div class="panel-body">

                    {{ form_errors(form) }}

                    {{ form_row(form.active) }}
                    {{ form_row(form.name) }}
                    {{ form_row(form.description) }}
                    {{ form_row(form.contact_name) }}
                    {{ form_row(form.contact_email) }}
                    {{ form_row(form.institution) }}
                    {{ form_row(form.service_description_url) }}

                    <p class="text-primary"> For ISA RLMS: </p>

                    {{ form_row(form.guid) }}
                    {{ form_row(form.service_url) }}

                    <p class="text-primary"> For Weblab Deusto RLMS: </p>

                    {{ form_row(form.rlms_username) }}
                    {{ form_row(form.rlms_password) }}

                </div>
            </div>
        </div>

    </div>

    {{ form_row(form.submit) }}
    {{ form_end(form) }}

{% endblock %}